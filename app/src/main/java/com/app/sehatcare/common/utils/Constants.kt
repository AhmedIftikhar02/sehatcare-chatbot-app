package com.app.sehatcare.common.utils

object Constants {
    const val NETWORK_TIMEOUT_SECONDS = 30L
    const val DEFAULT_PAGE_SIZE = 20

    // Used as the IntentExtra/Bundle key naming convention - keep all keys here so they're
    // never duplicated/typo'd across screens.
    const val KEY_PRODUCT_ID = "key_product_id"
}
